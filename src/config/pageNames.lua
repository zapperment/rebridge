-- The name of each parameter page, shown on the display along with the page
-- number when the page is switched. Keyed by the device type (as mapped to a
-- name in the remote map), then by page number, in the order the pages are
-- defined in the remote map's page group.
return {
  subtractor = {
    "Osc & Noise",
    "Filter & Amp",
    "LFO",
    "Perf & Mod",
  },
}
