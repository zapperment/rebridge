-- Records whether the Shift button on the remote surface (Launch Control) is
-- currently held down. Shift gates controls with a secondary function, such as
-- the page buttons browsing patches.
return {
  held = false
}
