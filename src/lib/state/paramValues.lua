-- The last known on/off settings of parameters that the display of other
-- parameters depends on (see config/conditionalValueLabels), keyed by
-- parameter name. Recorded by setState/buttons as the host (Reason) reports
-- them.
return {}
